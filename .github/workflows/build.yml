name: Route Metrics Pipeline

on:
  push:
    branches:
      - master
    paths-ignore:
      - '.gitignore'

jobs:
#  build:
#    runs-on: ubuntu-latest
#
#    services:
#      rabbitmq:
#        image: rabbitmq:3
#        ports:
#          - 5672:5672
#
#    steps:
#      - name: Checkout code
#        uses: actions/checkout@v3
#
#      - name: Set up JDK 17
#        uses: actions/setup-java@v3
#        with:
#          java-version: 17
#          distribution: 'adopt'
#          cache: gradle
#
#      - name: Build with Gradle (using flags)
#        run: gradle -b infrastructure/build.gradle -DprojectDir=infrastructure build
#
#      - name: Archive artifacts
#        uses: actions/upload-artifact@v2
#        with:
#          name: app-artifacts
#          path: infrastructure/build/libs/route-metrics-backend.jar
#
#  deploy-docker-hub:
#    runs-on: ubuntu-latest
#
#    needs: build
#
#    steps:
#      - name: Checkout code
#        uses: actions/checkout@v3
#
#      - name: Download artifacts
#        uses: actions/download-artifact@v2
#        with:
#          name: app-artifacts
#          path: infrastructure/build/libs/
#
#      - name: Set up Docker Buildx
#        uses: docker/setup-buildx-action@v2
#
#      - name: Login to Docker Hub
#        uses: docker/login-action@v2
#        with:
#          username: ${{ secrets.DOCKER_USER }}
#          password: ${{ secrets.DOCKER_PASSWORD }}
#
#      - name: Build and push
#        id: docker_build
#        uses: docker/build-push-action@v4
#        with:
#          context: .
#          file: Dockerfile
#          push: true
#          tags: torhugo/route-metrics:latest

  deploy-oracle-vps:
    runs-on: ubuntu-latest

    #needs: deploy-docker-hub

    steps:
#      - name: Checkout code
#        uses: actions/checkout@v3

      - name: Executing remote ssh commands using ssh key
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          script: |
            echo ola

#      - name: SSH into Oracle VPS
#        uses: webfactory/ssh-agent@v0.9.0
#        with:
#          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}
#
#      - name: Pull Docker images
#        run: ssh ${{ secrets.VPS_USERNAME }}@${{ secrets.VPS_HOST }} "cd ~/ && sudo docker-compose pull"
#
#      - name: Restart containers
#        run: ssh ${{ secrets.VPS_USERNAME }}@${{ secrets.VPS_HOST }} "cd ~/ && sudo docker-compose restart"