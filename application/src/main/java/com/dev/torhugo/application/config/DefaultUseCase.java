package com.dev.torhugo.application.config;

import java.util.logging.Logger;

public class DefaultUseCase {
    protected DefaultUseCase() {
    }
    protected final Logger logger = Logger.getLogger(getClass().getName());
}
